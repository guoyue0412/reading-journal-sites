import { useRef, useState, type ClipboardEvent, type ChangeEvent } from "react";
type Props={postId:string;label:string;value:string;onChange:(value:string)=>void};
export function MarkdownSectionEditor({postId,label,value,onChange}:Props){const ref=useRef<HTMLTextAreaElement>(null),input=useRef<HTMLInputElement>(null),[uploading,setUploading]=useState(false),[error,setError]=useState("");function insert(before:string,after=before,placeholder="文字"){const el=ref.current,start=el?.selectionStart??value.length,end=el?.selectionEnd??start;onChange(value.slice(0,start)+before+(value.slice(start,end)||placeholder)+after+value.slice(end));}async function upload(file:File){setUploading(true);setError("");try{const form=new FormData();form.set("postId",postId);form.set("file",file);const response=await fetch("/api/editor/assets",{method:"POST",body:form});const payload=await response.json() as {url?:string;safeName?:string;error?:string};if(!response.ok||!payload.url)throw new Error(payload.error||"图片上传失败");insert(`![${payload.safeName||"图片"}](${payload.url})`,"","");}catch(e){setError(e instanceof Error?e.message:"图片上传失败");}finally{setUploading(false);}}function paste(event:ClipboardEvent<HTMLTextAreaElement>){const file=[...event.clipboardData.files].find(f=>f.type.startsWith("image/"));if(file){event.preventDefault();void upload(file);}}function choose(event:ChangeEvent<HTMLInputElement>){const file=event.target.files?.[0];event.target.value="";if(file)void upload(file);}return <div className="markdown-editor"><div className="markdown-editor__toolbar" aria-label="Markdown 工具栏"><button type="button" onClick={()=>insert("**")}>粗体</button><button type="button" onClick={()=>insert("[","](https://)")}>链接</button><button type="button" onClick={()=>insert("$","$","E=mc^2")}>公式</button><button type="button" disabled={uploading} onClick={()=>input.current?.click()}>{uploading?"上传中…":"图片"}</button><input ref={input} hidden type="file" accept="image/png,image/jpeg,image/gif,image/webp" onChange={choose}/></div><textarea ref={ref} aria-label={label} value={value} onPaste={paste} onChange={e=>onChange(e.target.value)} placeholder="使用 Markdown 写作；支持图片粘贴、LaTeX 公式与 [[文章-slug]] 关联。"/>{error?<p className="markdown-editor__error" role="alert">{error}</p>:null}</div>}
